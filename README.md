# Simple API utilizando Express
Utilice Express y UUID para crear una simple API siguiendo el estándar CRUD. No utilice una base de datos real, sino simplemente un JSON que se crea al iniciar el servidor.
